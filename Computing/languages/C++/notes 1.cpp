// Udacity C++ for programmers

#include <iostream>
#include <fstream>
#include <string>
#include <sstream> 

// Hello world 

#include <iostream>

using namespace std;

int main() 
{
cout<<"Hello World!";
return 0; //not really needed?
}

using namespace std; // all elements in the standard c++ library

#include "main.hpp" // replacement of above (how the program does a task)
#include "main.cpp"; //code on what the program does ("c plus plus")


//File IO
ofstream; // writung
ifstream; //read
fstream; //reading and writing

myfile.open("filename");
myfile.close("filename");

std::cout; //writing to the console
std::cin; // reading from the console, cant recieve strings with a space
std::getline(std::cin, variableName); // retrieve chars from std::in until /n


// Arithmetic Operations
# include<cmath>

// exponention
std::pow(base, exp)

// Pi
M_PI

// typecastig is essential for programming, if not, the type name should always have type in





